% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tranform_assay.R
\name{impute_x_approximate_distribution}
\alias{impute_x_approximate_distribution}
\title{Infer the approximate counts distribution of a sample}
\usage{
impute_x_approximate_distribution(df, counts_gap_threshold, pos_mode_threshold)
}
\arguments{
\item{df}{A data frame of per-sample count metrics.}

\item{counts_gap_threshold}{Minimum positive-count gap (min/mean) that
distinguishes double-log from single-log data. Default: \code{0.25}.}

\item{pos_mode_threshold}{Positive-count mode must exceed this value (together
with the gap threshold) to call \code{double_log1p}. Default: \code{1}.}
}
\value{
\code{df} with an \code{inferred_distribution} column. Possible values:
\code{double_log1p}, \code{log1p}, \code{raw_scaled}, \code{raw}, \code{log_negative_max_10},
\code{raw_negative_scaled}, or \code{NA} when no rule matches.
}
\description{
Decision tree used to infer Census sample distribution and to classify each sample's
expression assay before choosing an HPCell transform
(\code{safe_expm1}, \code{expm1}, or \code{identity}).
}
\details{
Required columns: \code{has_negative}, \code{all_integer}, \code{counts_gap_min_mean},
\code{positive_mode}, and either \code{is_gene_expression_likely_log}, \code{has_rounding_error}
}
