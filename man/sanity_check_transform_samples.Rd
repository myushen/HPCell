% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sanity_check_transformed_samples.R
\name{sanity_check_transform_samples}
\alias{sanity_check_transform_samples}
\alias{sanity_check_transform_samples.HPCell}
\title{sanity check Samples with Nonsensical Count Values After Transformation}
\usage{
sanity_check_transform_samples(
  input_hpc,
  target_input = "sce_transformed",
  target_output = "sanity_checked_sample_stats",
  n_cells_sample = 5000L,
  ...
)

\method{sanity_check_transform_samples}{HPCell}(
  input_hpc,
  target_input = "sce_transformed",
  target_output = "sanity_checked_sample_stats",
  n_cells_sample = 5000L,
  ...
)
}
\arguments{
\item{input_hpc}{An \code{HPCell} object.}

\item{target_input}{Name of the targets target providing the transformed
\code{SingleCellExperiment} objects. Default: \code{"sce_transformed"}.}

\item{target_output}{Base name for the output targets.  The pipeline appends
\verb{_combined} for the merged tibble and \verb{_report} for the HTML report.
Default: \code{"sanity_checked_sample_stats"}.}

\item{n_cells_sample}{Integer. Maximum number of cells to subsample per
sample when computing the check statistics. Default: \code{5000L}.}

\item{...}{Additional arguments (unused; for method dispatch).}
}
\value{
The updated \code{HPCell} object with three new pipeline steps
appended (iterate, merge, report).
}
\description{
Adds a per-sample sanity-check step to the HPCell pipeline, followed by a
cross-sample merge and an HTML quality-control report.  Three independent
binary sanity checks are computed for each sample:

\describe{
\item{\code{sanity_check_max_lt_10}}{Maximum count value is < 10, suggesting the data
were never back-transformed out of log/fractional space.}
\item{\code{sanity_check_min_lt_0}}{Minimum count value is negative, which should not
occur after the mode-subtraction and floor-clipping in
\code{\link{transform_utility}}.}
\item{\code{sanity_check_rounding_error}}{Counts are not all exact integers but every
value is within \eqn{10^{-4}} of an integer — tiny floating-point
artefacts from numerical operations.  Exact-integer samples are excluded.}
}

A sample can carry any combination of sanity checks.  The resulting target
(\code{target_output}) is a list of 1-row tibbles (or \code{NULL} for empty samples);
an aggregate tibble is written to \verb{<target_output>_combined}, and an HTML
report is written to \verb{<target_output>_report}.
}
