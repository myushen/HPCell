# renv is available for reproducible environments.
# To activate, run: renv::activate() or set RENV_AUTOLOAD_ENABLED=TRUE
if (identical(Sys.getenv("RENV_AUTOLOAD_ENABLED"), "TRUE")) {
  source("renv/activate.R")
}
